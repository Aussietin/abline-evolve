{
  "name": "abline-evolve",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --import \"data:text/javascript,import { register } from 'node:module'; import { pathToFileURL } from 'node:url'; register('./scripts/ts-extension-hook.mjs', pathToFileURL('./'));\" scripts/sim-tests.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^8.2.2"
  }
}
